#include "control_system.h"

void setRudder(float percentage) {
    //Call lower level driver function to set the rudder position based on the percentage input. 
	//May need to add another input value for left/right (bool)
}

void setSpeed(float percentage) {
	//Call lower level driver function to set the motor speed based on the percentage of the maximum speed output by the AI
    
}